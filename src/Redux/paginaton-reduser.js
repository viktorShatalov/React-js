const PAGINATION = 'PAGINATION';

let paginationState = {
    users:[],
    currentPage:1,
    userPerPage:10
}
const paginationReducer = (state= paginationState,action) => {
    switch (action.type){
        
    }
}